import userMenu from './userMenu';

describe('User Menu directive', () => {
    it('should create the proper DOM structure', () => {
        expect(true).toBe(true);
        console.log(userMenu);
    });
});
